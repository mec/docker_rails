on: [push]

jobs:
  build:
    name: build
    runs-on: ubuntu-latest
    steps:

    - name: Checkout
      uses: actions/checkout@v2

    - name: build and test code
      run: DOCKER_BUILDKIT=1 build .

